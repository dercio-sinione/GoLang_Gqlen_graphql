type User {
  id: ID!
  username: String!
  email: String!

  meetups: [Meetups!]!
}

type Meetups {
  id: ID!
  name: String!
  description: String!
  user: User!
}

type Query {
  meetups: [Meetups!]!
}
